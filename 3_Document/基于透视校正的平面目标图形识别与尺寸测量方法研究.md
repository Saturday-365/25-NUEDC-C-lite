# 基于 PnP 的 A4 目标板相对相机位姿解算方法

## 摘要

本文算法最初是为了完成 2025 年全国大学生电子设计竞赛 C 题中的单目视觉测量任务。C 题目标物为带 2 cm 黑色边框的竖立 A4 纸，这一结构提供了稳定的四个已知角点。本文在原有测距思路上进一步拓展，不只计算目标板到相机的前向距离，而是以相机为三维坐标系原点，利用 `solvePnP` 求解 A4 目标板中心在相机坐标系下的具体坐标 $(X,Y,Z)$，同时计算目标板相对相机的俯仰角、偏航角和滚转角。这样，算法既能满足电赛 C 题对距离 $D$ 的要求，也能用于后续目标板空间定位和姿态分析。

**关键词：** C 题；单目视觉；PnP；相机坐标系；目标板位姿；A4 外框

## 1 引言

2025 年电赛 C 题的任务是制作一个“基于单目视觉的目标物测量装置”。题目要求装置能够测量并显示两个主要量：一个是基准线到目标物的距离 $D$，另一个是目标物平面上几何图形的边长或直径 $x$。从题目描述看，目标物不是随意物体，而是竖立的白色 A4 纸，四边印有线宽 2 cm 的黑色边框线，底边中点处还有标记点。这个特征对视觉方案很关键，因为黑色外框可以作为稳定的定位依据。

题目还明确限制只能使用一个摄像头，不能使用超声、激光、毫米波雷达等非视觉方式测距，也不能使用 PC 机作为测量装置。因此，最开始的算法目标是：只依靠单目摄像头，从图像中得到距离 $D$ 和图形尺寸 $x$。其中距离 $D$ 的测量范围为 100 cm 至 200 cm，基本要求误差不超过 5 cm，发挥部分要求误差不超过 2 cm。这部分要求是本文算法的基础来源。

在这个基础上，本文把研究重点从单一距离测量拓展为目标板位姿解算。具体来说，仍然利用 A4 外框的已知尺寸建立三维点，并在图像中检测对应的二维角点；再结合相机内参，用 OpenCV 的 `solvePnP` 求出旋转向量和平移向量。平移向量表示目标板中心在相机坐标系下的 $X$、$Y$、$Z$ 坐标，旋转向量可以转换为目标板相对相机的姿态角。这样既保留了 C 题所需的距离信息，也能得到更完整的空间位置关系。

## 2 C 题基础与拓展目标

### 2.1 目标物特点

题目说明中给出的目标物有几个重要特点。第一，所有目标物面都是竖立的白色 A4 纸。第二，A4 纸四边都有 2 cm 宽的黑色边框线。第三，基本目标物中心位置分别印有圆形、等边三角形、正方形，直径或边长范围为 10 cm 至 16 cm。第四，发挥部分会出现正方形组合、局部重叠正方形、带数字编号的正方形等情况，边长范围为 6 cm 至 12 cm。

这些要求说明，目标物有两个层次的信息需要处理：外层是 A4 黑色边框，用于确定目标物的位置、姿态和距离；内层是黑色实心几何图形，用于测量 $x$。如果把两部分混在一起处理，程序会变复杂。更合理的做法是先稳定识别外框，完成距离和透视校正，再在校正后的物面图像中处理内部图形。

### 2.2 从距离 D 到三维坐标

C 题中的距离 $D$ 是基准线到目标物投影点之间的距离。在原始比赛任务中，程序只要给出一个距离值即可。但从算法角度看，`solvePnP` 实际上并不是只输出一个距离，而是输出目标板相对相机的完整平移向量。

本文以相机光心作为三维坐标系原点，建立相机坐标系。一般可理解为：$X$ 轴表示相机画面左右方向，$Y$ 轴表示相机画面上下方向，$Z$ 轴表示相机光轴前方。若把 A4 目标板坐标系原点取在外框中心，则 `tvec=[t_x,t_y,t_z]^T` 就表示目标板中心相对相机的位置。此时 $t_z$ 可以继续作为距离测量的主要依据，而 $t_x$ 和 $t_y$ 则说明目标板相对画面中心的横向和纵向偏移。

### 2.3 图形尺寸 x 的含义

题目中的 $x$ 表示物面上几何图形的边长或直径。基本部分中，目标物可能是圆形、等边三角形或正方形；发挥部分中，主要关注正方形，可能要求测量最小面积正方形或指定编号正方形的边长。本文重点放在距离 $D$，但仍保留 $x$ 的基本处理思路：先利用外框角点做透视校正，把倾斜拍摄的 A4 纸校正成近似正视图；再根据 A4 纸真实尺寸建立像素到厘米的比例关系；最后由内部图形轮廓计算边长或直径。

## 3 PnP 相对位姿解算原理

### 3.1 PnP 算法的基本思想

单目摄像头拍到的是二维图像，图像本身没有直接的深度信息。如果只看一个像素点，无法判断它离摄像头有多远。但 C 题目标物的 A4 外框尺寸是已知的，四个角点在真实物体上的位置可以提前写出来，图像中也能检测到这四个角点。这样就把问题变成了 PnP 问题。

PnP 的全称可以理解为 Perspective-n-Point，意思是根据若干个三维空间点和它们在图像中的二维投影点，求出物体相对相机的姿态。这里的“姿态”包括两部分：一部分是旋转，也就是目标物相对摄像头有没有偏转；另一部分是平移，也就是目标物相对摄像头在左右、上下和前后方向上的位置。OpenCV 中的 `solvePnP` 函数就是完成这一解算的常用接口。

### 3.2 坐标关系和投影模型

PnP 解算中至少要明确三个量。第一个量是目标物上的三维点，本文取 A4 外框四个角点；第二个量是图像中的二维点，也就是程序检测到的四个角点像素坐标；第三个量是相机内参，包括焦距和主点位置。相机内参可以通过标定得到，它决定了相机如何把空间点投影到图像平面。

从几何关系上看，一个目标物坐标系中的点 $P_w$，先经过旋转和平移变换到相机坐标系，再通过相机内参投影到图像平面。可以简单写成：

```text
imagePoint = cameraMatrix * [R | t] * objectPoint
```

其中 $R$ 表示旋转矩阵，$t$ 表示平移向量，`cameraMatrix` 表示相机内参矩阵。`solvePnP` 要做的事情，就是在已知 `objectPoints`、`imagePoints` 和 `cameraMatrix` 的情况下，反推出 $R$ 和 $t$。

### 3.3 三维点和二维点的建立

以 A4 纸中心作为目标物坐标系原点，设外框实际宽度为 $W$，高度为 $H$，则四个角点的三维坐标可写为：

```text
左上角：(-W/2, -H/2, 0)
右上角：( W/2, -H/2, 0)
右下角：( W/2,  H/2, 0)
左下角：(-W/2,  H/2, 0)
```

这些点对应 `solvePnP` 中的 `objectPoints`。图像处理检测到的四个外框角点则对应 `imagePoints`。这里最容易出错的是角点顺序。如果三维点按左上、右上、右下、左下排列，二维点也必须按同样顺序排列。否则程序虽然可能输出一个距离值，但这个值没有实际意义。

因为 C 题的目标物是平面 A4 纸，四个角点都在同一个平面上，所以代码中更适合使用面向平面目标的求解方式，例如 `SOLVEPNP_IPPE`。它比较适合由矩形平面角点估计位姿的场景。对于本文的方案来说，真正重要的不是把所有 PnP 分支算法都讲完整，而是说明清楚：A4 外框提供了稳定的四个已知点，PnP 根据这四个点求出目标物相对摄像头的位置。

### 3.4 平移向量与目标板坐标

`solvePnP` 输出旋转向量 $rvec$ 和平移向量 $tvec$。其中平移向量可表示为：

```text
tvec = [tx, ty, tz]^T
```

$tx$ 和 $ty$ 表示目标板中心在相机坐标系中的横向和纵向偏移，$tz$ 表示目标板中心沿摄像头光轴方向的深度。因此本文不再只保留 $tz$，而是把三者都作为算法输出：

```text
boardPosition = (X, Y, Z) = (tx, ty, tz)
```

其中 $Z$ 分量仍然可以作为电赛 C 题中的原始距离 $D_{raw}$ 使用，$X$ 和 $Y$ 则用于描述目标板中心相对相机原点的空间偏移。这样处理后，算法从“测距”扩展为“定位”。

旋转向量 $rvec$ 可以通过 Rodrigues 变换转换成旋转矩阵，再进一步计算俯仰角、偏航角和滚转角。这样就能知道目标板相对相机是否左右偏转、上下俯仰或发生平面内旋转。对于电赛 C 题来说，姿态角可以帮助解释倾斜目标物带来的误差；对于拓展应用来说，姿态角本身就是目标板空间状态的一部分。

## 4 方法设计

### 4.1 总体流程

面向 C 题的处理流程如下：

```text
摄像头采集图像
  ↓
提取 A4 黑色外框
  ↓
排序四个外框角点
  ↓
solvePnP 求解目标板坐标和姿态
  ↓
提取 X/Y/Z 坐标，并保留 Z 作为距离量
  ↓
透视校正物面图像
  ↓
辅助测量图形尺寸 x
```

这个流程把外框放在核心位置。外框角点既用于 PnP 位姿解算，也用于透视校正。内部图形识别放在后面处理，作为 C 题尺寸测量部分的补充；本文重点则放在目标板相对相机的三维坐标和姿态角。

### 4.2 外框检测

外框检测不是本文重点，因此只做简要说明。目标物外框是 2 cm 宽的黑色边框，和白色 A4 纸背景有明显对比。程序通过阈值分割和轮廓筛选找到最大的近似四边形区域，再把它作为 A4 外框。这里不再展开灰度化、二值化、形态学处理等基础步骤，论文重点放在外框角点如何进入 PnP 测距。

### 4.3 角点排序

得到四个角点后，需要将它们排序为左上、右上、右下、左下。常用方法是利用坐标和与坐标差：坐标和最小的是左上角，坐标和最大的是右下角，坐标差最小的是右上角，坐标差最大的是左下角。

角点排序对测距很重要。`solvePnP` 本质上是在匹配真实角点和图像角点，如果顺序错了，相当于把目标物几何关系弄错，距离和姿态都会不可靠。因此实际调试时，最好把角点编号画在图像上，确认顺序正确后再进行测距。

### 4.4 solvePnP 求解相对坐标和姿态

角点准备好后，调用 `solvePnP`：

```text
solvePnP(objectPoints, imagePoints, cameraMatrix, distCoeffs,
         rvec, tvec, false, SOLVEPNP_IPPE)
```

其中 `cameraMatrix` 是相机内参矩阵，`distCoeffs` 是畸变系数。`objectPoints` 来自 A4 外框的真实尺寸，`imagePoints` 来自图像角点检测。因为目标物是平面 A4 纸，代码中使用 `SOLVEPNP_IPPE` 方法更合适。求解成功后，程序读取 `tvec` 的三个分量：

```text
paperPositionX = tvec_x
paperPositionY = tvec_y
paperPositionZ = tvec_z
paperDistanceRaw = paperPositionZ
```

这样，`paperPositionX/Y/Z` 表示 A4 目标板中心在相机坐标系下的具体坐标，单位与 A4 外框真实尺寸一致，代码中使用毫米。`paperDistanceRaw` 继续保留，用于兼容原来的 C 题距离测量逻辑。与此同时，根据 $rvec$ 计算俯仰角、偏航角和滚转角，得到目标板相对相机的姿态。

### 4.5 距离修正与坐标输出

题目要求基本部分 $D$ 的误差不超过 5 cm，发挥部分不超过 2 cm。单纯依靠 `solvePnP` 原始输出，可能会因为相机标定、镜头畸变和角点误差产生偏差。因此，原 C 题测距部分仍然可以对 $Z$ 分量做距离修正。

本文采用分段修正思路。先在 100 cm、125 cm、150 cm、175 cm、200 cm 等已知距离处采集样本，记录原始测距值和真实距离的差值，形成偏移表。实际运行时，根据当前原始距离所在区间，对相邻偏移量做线性插值，得到修正量。可简单表示为：

```text
D_cal = D_raw - offset
```

更完整地写为：

```text
D_cal = D_raw - offset(i) - alpha * (offset(i+1) - offset(i))
```

其中 $alpha$ 表示当前距离在该区间内的位置比例。这样可以分别补偿近距离和远距离误差，比固定加减一个常数更可靠。需要说明的是，距离修正主要作用在 $Z$ 分量上；$X$、$Y$ 坐标和姿态角更依赖相机标定质量、角点检测精度和目标板模型是否准确。

### 4.6 图形尺寸 x 的辅助测量

距离 $D$ 是本文重点，但 C 题还要求测量 $x$。对于基本目标物，$x$ 是圆直径、等边三角形边长或正方形边长；对于发挥目标物，$x$ 多数是某个正方形的边长。本文的处理思路是：先利用 A4 外框四角点做透视校正，把物面变成近似正视图；再根据 A4 外框真实尺寸和校正图像像素尺寸建立比例关系；最后对内部黑色图形进行轮廓检测，计算边长或直径。

由于本文主要讨论测距，图形识别部分只保留必要说明。实际比赛中，如果要完成发挥部分的编号正方形，还需要增加数字识别或指定区域筛选，这部分可以作为后续改进。

## 5 实验设计与评价

### 5.1 实验数据

为了贴合 C 题，实验距离应覆盖 100 cm 至 200 cm。可以在轴线上放置目标物，分别采集 100 cm、125 cm、150 cm、175 cm、200 cm 等距离下的图像。同时，为了测试姿态影响，可以让目标物在某些距离处产生一定角度偏转，观察测距结果是否明显变差。

| 数据组 | 真实距离 | 目的 |
|---|---:|---|
| distance_100cm | 100 cm | 检查近端测距 |
| distance_125cm | 125 cm | 建立修正表 |
| distance_150cm | 150 cm | 检查中间距离 |
| distance_175cm | 175 cm | 建立修正表 |
| distance_200cm | 200 cm | 检查远端测距 |
| angle_30 | 任选距离 | 检查倾斜影响 |

### 5.2 评价指标

根据当前拓展方向，评价指标不只围绕 $D$ 和 $x$，还应加入目标板三维坐标和姿态角：

| 指标 | 说明 |
|---|---|
| $Z$ 方向误差 | 目标板前向距离误差，可对应 C 题距离 $D$ |
| $X/Y$ 坐标偏移 | 目标板中心相对相机原点的横向、纵向偏移 |
| 姿态角误差 | 目标板俯仰角、偏航角、滚转角与实际角度的差值 |
| $D$ 基本要求 | 原 C 题距离误差不超过 5 cm |
| $D$ 发挥要求 | 原 C 题距离误差不超过 2 cm |
| $x$ 测量误差 | 保留对内部图形边长或直径的测量评价 |
| 用时 | 一键启动到显示结果不超过 5 s |

实验记录表可设计为：

| 图片编号 | 真实Z/cm | 输出X/mm | 输出Y/mm | 输出Z/mm | 偏航角/° | 俯仰角/° | 备注 |
|---|---:|---:|---:|---:|---:|---:|---|
| distance_100cm | 100 | 待填 | 待填 | 待填 | 待填 | 待填 | 正放测试 |
| distance_150cm | 150 | 待填 | 待填 | 待填 | 待填 | 待填 | 中距离测试 |
| angle_30 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 倾斜目标板测试 |

### 5.3 测试图片预留

为了让论文后期更容易补充实物测试结果，本节预留测试图片位置。图片可以选择三类：第一类是摄像头采集到的原始目标物图像，用来说明 A4 目标物和黑色外框在画面中的状态；第二类是程序识别外框角点后的图像，用来说明测距使用的四个角点是否稳定；第三类是透视校正后的物面图像，用来说明后续测量图形尺寸 $x$ 的基础。

【图 1 预留位置：100 cm 距离下目标物原始采集图像】

【图 2 预留位置：A4 外框检测与四角点标注结果】

【图 3 预留位置：透视校正后的目标物平面图像】

【图 4 预留位置：测距结果与显示界面截图】

## 6 误差分析

第一类误差来自外框角点。C 题的目标物有 2 cm 黑框，这对检测有帮助，但如果光照不均、边框反光或摄像头分辨率较低，角点位置仍可能偏移。`solvePnP` 只使用四个角点，角点偏差会直接影响 $tvec$ 和 $rvec$，最终影响目标板的 $X/Y/Z$ 坐标和姿态角。

第二类误差来自相机标定。相机内参不准确时，图像点反投影方向会有偏差，不仅 $Z$ 方向距离会出错，$X/Y$ 坐标也会随之偏移。若希望把算法从 C 题测距拓展到稳定的位置坐标输出，相机标定比单纯测距时更加重要。

第三类误差来自坐标系定义。本文把相机作为三维坐标系原点，把 A4 目标板中心作为目标板坐标系原点，因此输出坐标表示的是“目标板中心相对相机”的位置。如果后续要严格对应 C 题中的底边标记点，就需要把目标板坐标系原点改到底边标记点，或者在输出后加入固定几何偏移。

第四类误差来自目标物姿态。发挥部分中目标物可能水平转动，使物面与轴线成 30° 至 60° 夹角。这时外框在图像中透视变形更明显，角点检测更难，PnP 解算也更敏感。因此，如果要完成发挥要求，需要更稳定的角点提取，并尽量使用相机畸变校正后的图像。

## 7 结论

本文以 2025 年电赛 C 题为算法基础，但不再只停留在距离 $D$ 的测量上，而是进一步拓展到 A4 目标板相对相机的完整位姿解算。针对目标物为 A4 纸、四边有 2 cm 黑框的特点，本文利用外框四角点建立三维点与二维图像点的对应关系，并通过 `solvePnP` 求解目标板相对相机的平移向量和旋转向量。

在算法输出上，$tvec$ 的三个分量被保存为目标板中心在相机坐标系下的 $X/Y/Z$ 坐标，其中 $Z$ 分量继续用于原 C 题距离测量；$rvec$ 则被转换为俯仰角、偏航角和滚转角，用来描述目标板相对相机的角度状态。这样处理后，方案既能解释原电赛 C 题的测距需求，也能支撑更一般的目标板空间定位。后续若继续优化，可以重点提高相机标定精度、改进角点检测稳定性，并增加不同距离和不同角度下的坐标验证实验。
